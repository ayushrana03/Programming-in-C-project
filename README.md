# Matrix Calculator - C

## Overview
A modular Matrix Calculator in C that supports:
- Addition, subtraction, multiplication
- Transpose
- Determinant (recursive Laplace expansion)
- Save result to a text file (`matrix_results.txt`)

Uses dynamic memory, pointers, structures, functions, file I/O and recursion.

